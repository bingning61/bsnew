# 论文最终成稿前的缺失信息清单

下表用于明确：哪些关键材料当前仓库还没有提供，为什么论文需要它，以及后续应该如何补充。凡是表中标记为“当前仓库未提供”的项目，后续写论文时都不能被当成既定事实直接虚构。

| 缺失项 | 为什么论文需要它 | 当前仓库是否提供 | 后续如何补 |
| --- | --- | --- | --- |
| 实际 YOLO 权重文件路径与文件本体 | 第四章运行验证和复现说明必须给出真实模型来源 | 未提供 | 回到原环境后确认实际 `.pt` 文件，并记录路径、文件名和使用版本 |
| 训练数据集配置文件与标签定义 | 第二章若要说明目标类别、第四章若要说明训练背景，必须有标签依据 | 未提供 | 补充数据集 `yaml`、类别列表、样本说明和标注示例 |
| 焊缝目标类别编号与 `target_class_id` 对应关系 | 关系到第二章方法描述与运行参数解释 | 未明确提供 | 通过训练数据或实际运行参数确认类别编号含义 |
| 实机底盘型号 | 第四章实验平台介绍需要真实硬件名称 | 未提供 | 补充底盘型号、厂家或平台名称，并拍摄实物照片 |
| 实际相机型号与安装方式 | 第二章输入设备说明和第四章实验平台说明需要 | 未明确提供 | 补充相机型号、分辨率、安装位置和安装照片 |
| `BASE_TYPE`、`CAMERA_TYPE` 的真实取值 | 影响 `robot_camera.launch` 和相关 TF、标定配置 | 未明确提供 | 在原环境中执行 `echo $BASE_TYPE`、`echo $CAMERA_TYPE` 并记录 |
| ROS1 实际运行截图 | 第四章系统实现和答辩展示需要直观证据 | 未提供 | 补充 `roslaunch` 终端截图、`rqt_graph` 截图和 RViz/图像窗口截图 |
| `/seam_center` 话题回显截图 | 证明视觉结果已正确转化为控制中间量 | 未提供 | 在原环境执行 `rostopic echo /seam_center` 并截图 |
| `cmd_vel` 话题回显截图 | 证明控制节点已输出速度指令 | 未提供 | 在原环境执行 `rostopic echo /cmd_vel` 并截图 |
| 检测效果截图 | 第二章和第四章都需要说明视觉方法实际表现 | 未提供 | 运行系统后截图保存 `/result_image` 画面 |
| 实机跟踪过程照片或连续截图 | 第四章实验部分需要展示机器人跟踪现象 | 未提供 | 拍摄机器人沿焊缝跟踪时的照片或视频截帧 |
| 定量实验数据 | 论文第四章若要有说服力，必须有识别或跟踪效果统计 | 未提供 | 至少补充成功率、偏差变化、停止响应或现象统计表 |
| 安全停止验证记录 | 第三章和第四章中安全机制需要实验支撑 | 未提供 | 人工设计无检测和断流场景，记录机器人是否及时停机 |
| 焊缝专用仿真场景 | 若要把“仿真分析”写得更完整，必须有焊缝相关视觉环境 | 未提供 | 在 Gazebo 或其他环境中补充焊缝场景模型、相机视角和运行记录 |
| 仿真联调截图或视频 | 第四章仿真分析需要可视证据，而不仅是 launch 文件存在 | 未提供 | 补充 Gazebo/Stage 运行截图、仿真话题截图和节点协同截图 |
| 硬件参数表 | 论文正文和附录常需要实验平台参数 | 未提供 | 整理底盘尺寸、传感器参数、供电方式、串口参数等信息 |
| 相机内参是否对应实际部署相机 | 第二章写标定信息时必须知道是否真实使用 `astrapro.yaml` 或 `csi72.yaml` | 当前仓库仅有标定文件，未说明实际采用哪一个 | 在原环境中确认 `CAMERA_TYPE` 和实际相机 |
| Python 依赖共存说明 | 当前主链路混用 `python3` 与 `python` 风格脚本，运行环境说明需要真实依据 | 未明确提供 | 记录原环境中 `python`、`python3`、`rospy`、`cv_bridge`、`torch`、`ultralytics` 的安装方式 |
| 代码修改历史或整合过程说明 | 若导师或答辩老师追问“做了哪些工作”，需要有工程过程辅助说明 | 未系统提供 | 根据 git 历史、旧文档或人工回忆整理时间线，但不要把它写成论文主线 |
| 视频与帧图像的来源说明 | 若要在论文中引用 `原视频.mp4`、`识别视频.mp4` 或 `yolo/frames/`，需说明来源和用途 | 未明确提供 | 人工确认这些媒体是训练素材、测试素材还是结果展示素材 |

## 使用建议

如果后续准备让新的 GPT 帮你写第四章或答辩 PPT，请先把这份清单一起发过去。这样新的 GPT 会知道哪些材料已经有依据，哪些内容必须留空、标注待补或只写成实验设计方案，而不会擅自虚构结果。
